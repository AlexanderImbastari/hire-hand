'use client';

import { UserDashboard } from '@/components/UserDashboard';

/** Homeowner home: every job they have posted. Guarded by the group layout. */
export default function HomeownerJobsPage() {
  return <UserDashboard />;
}
